# QA 2026-04-29

Rodada executada apos ajustes no seletor de paletas e recriacao do `DESIGN.md`.

## Resultado geral

Estado: verde para continuar.

Os motores JavaScript passam em checagem sintatica, manifests continuam validos, assets principais do HTML existem, assets do service worker existem e as views principais seguem conectadas.

## Checagens executadas

- `node --check app.js`
- `node --check template-engine.js`
- `node --check precision-engine.js`
- `node --check voice-engine.js`
- `node --check service-worker.js`
- `node --check lexical-engine.js`
- `node --check proof-engine.js`
- `node --check vrda-engine.js`
- `node --check backup-engine.js`
- `node --check archive-engine.js`
- `node --check version-engine.js`
- `node --check export-engine.js`
- validacao JSON de `manifest.webmanifest`
- validacao JSON de `favicon_io/site.webmanifest`
- varredura de referencias `src` e `href` em `index.html`
- varredura dos assets em `CORE_ASSETS` do service worker
- contagem dos guias em `template-engine.js`
- checagem de `data-view-target` contra `data-view-panel`
- checagem basica de `data-action` referenciados em `app.js`

## Resultado dos checks estruturais

- Referencias locais do HTML encontradas: 18.
- Assets do service worker encontrados: 23.
- Views conectadas: 5.
- Acoes `data-action` referenciadas em `app.js`: 24.
- Guias cadastrados: 57.

Distribuicao dos guias:

- Ficcao: 8
- Roteiro: 6
- Poesia: 4
- Nao ficcao: 4
- Jornalismo: 4
- Comercial e tecnica: 7
- Estudo e vestibular: 12
- Mercado editorial: 6
- Objeto livro: 6

## Alertas

- Resolvido depois desta rodada: as ocorrencias de `#fff` em componentes foram substituidas por variaveis semanticas. Restam apenas valores claros declarados como variaveis de tema.
- Esta rodada nao incluiu navegador real nem screenshot. O proximo QA visual deve abrir Editor, Arquivo, Academia e painel direito em pelo menos uma paleta clara e uma escura.

## Complemento: contraste e favicon

Rodada adicional apos correcao de contraste:

- `node --check app.js`
- `node --check service-worker.js`
- validacao JSON de `manifest.webmanifest`
- validacao JSON de `favicon_io/site.webmanifest`
- varredura de referencias locais do HTML
- varredura dos assets em `CORE_ASSETS`
- checagem de pixel do favicon 32

Resultado:

- Referencias locais do HTML encontradas: 18.
- Assets do service worker encontrados: 24.
- Favicon atualizado para verde pastel `#bfe3cf`.
- Cache offline atualizado para `vereda-offline-v33`.

## Complemento: reset forte de cache

Rodada adicional apos cache-busting dos assets:

- `node --check app.js`
- `node --check service-worker.js`
- validacao JSON de `manifest.webmanifest`
- varredura de referencias locais do HTML com query string
- varredura dos assets em `CORE_ASSETS` com query string

Resultado:

- HTML chama manifest, favicon, CSS e scripts com `?v=20260429-2`.
- Service worker atualizado para `vereda-offline-v34`.
- Assets do service worker encontrados: 24.

## Complemento: favicon dedicado da aba

Rodada adicional apos gerar favicon exclusivo para a aba do navegador:

- `identify` dos arquivos `tab-favicon-*`
- `node --check app.js`
- `node --check service-worker.js`
- varredura de referencias locais do HTML com query string
- varredura dos assets em `CORE_ASSETS` com query string

Resultado:

- `tab-favicon-16x16.png`: 16x16
- `tab-favicon-32x32.png`: 32x32
- `tab-favicon-48x48.png`: 48x48
- `tab-favicon.ico`: 16, 32 e 48
- HTML aponta os favicons da aba para `?v=20260429-3`.
- Service worker atualizado para `vereda-offline-v35`.
- Assets do service worker encontrados: 24.

## Complemento: novo reset de cache

Rodada adicional para forcar nova busca dos assets publicados:

- `node --check app.js`
- `node --check service-worker.js`
- validacao JSON de `manifest.webmanifest`
- varredura de referencias locais do HTML com query string
- varredura dos assets em `CORE_ASSETS` com query string

Resultado esperado:

- HTML aponta favicon, manifest, CSS e scripts para `?v=20260429-4`.
- Service worker atualizado para `vereda-offline-v36`.

## Complemento: respiro no favicon da aba

Rodada adicional apos regenerar `tab-favicon-*` com margem visual:

- `identify` dos arquivos `tab-favicon-*`
- `node --check app.js`
- `node --check service-worker.js`
- varredura de referencias locais do HTML com query string
- varredura dos assets em `CORE_ASSETS` com query string

Resultado esperado:

- favicon da aba menos encostado nas bordas do quadrado do navegador;
- HTML aponta favicon, manifest, CSS e scripts para `?v=20260429-5`;
- service worker atualizado para `vereda-offline-v37`.

## Complemento: Vocabulário Decolonizador

Rodada adicional apos integrar a ferramenta consultiva na Academia:

- `node --check app.js`
- `node --check decolonial-engine.js`
- `node --check service-worker.js`
- validacao JSON de `manifest.webmanifest`
- varredura de referencias locais do HTML com query string
- varredura dos assets em `CORE_ASSETS` com query string

Resultado esperado:

- `decolonial-engine.js` carrega 18 entradas locais;
- HTML aponta scripts e estilos para `?v=20260429-6`;
- service worker atualizado para `vereda-offline-v38`;
- ferramenta aparece na Academia entre Espelho de Voz e Guia de Oficios.

## Complemento: rolagem independente do Guia de Escrita

Rodada adicional apos limitar a altura do painel de guia no editor:

- `node --check app.js`
- `node --check service-worker.js`
- validacao JSON de `manifest.webmanifest`
- varredura de referencias locais do HTML com query string
- varredura dos assets em `CORE_ASSETS` com query string

Resultado esperado:

- painel `template-reference` tem rolagem vertical independente;
- comportamento mobile continua sem altura travada;
- HTML aponta scripts e estilos para `?v=20260429-7`;
- service worker atualizado para `vereda-offline-v39`.

## Complemento: marca final com contraste por paleta

Rodada adicional apos aplicar os SVGs finais da marca:

- `node --check app.js`
- `node --check service-worker.js`
- validacao JSON de `manifest.webmanifest`
- varredura de referencias locais do HTML com query string
- varredura dos assets em `CORE_ASSETS` com query string
- checagem de dimensoes dos PNGs/ICO gerados

Resultado esperado:

- site usa `vereda-logo-light.svg` nas paletas claras;
- site usa `vereda-logo-dark.svg` nas paletas escuras;
- aba aponta para `icons/vereda-logo-dark.svg?v=20260429-8`;
- HTML aponta scripts e estilos para `?v=20260429-8`;
- service worker atualizado para `vereda-offline-v40`.

## Estado de trabalho

Arquivos modificados no momento desta rodada:

- `DESIGN.md`
- `PRODUCT_LOG.md`
- `app.js`
- `index.html`
- `styles.css`

Arquivos nao rastreados ja conhecidos:

- `.codex`
- `QA_2026-04-28.md`
- `QA_2026-04-29.md`
- `logoVeredaok.psd`

## Complemento: QA visual mobile e aba clara

Rodada adicional apos corrigir os pontos 1 a 4 da revisao visual:

- `node --check app.js`
- `node --check decolonial-engine.js`
- `node --check service-worker.js`
- validacao JSON de `manifest.webmanifest`
- varredura de referencias locais do HTML com query string
- varredura dos assets em `CORE_ASSETS` com query string

Resultado esperado:

- aba aponta para `icons/vereda-logo-light.svg?v=20260429-9`;
- HTML aponta scripts e estilos para `?v=20260429-9`;
- service worker atualizado para `vereda-offline-v41`;
- no mobile, o editor vem antes do Guia de escrita;
- tabs superiores podem rolar horizontalmente sem corte visual;
- Academia usa escala menor e mais respiro em telas estreitas.

## Complemento: Observador Decolonial no manuscrito

Rodada adicional apos dissolver o arquivo `observador_decolonial_v3_full.html`:

- `node --check app.js`
- `node --check decolonial-engine.js`
- `node --check service-worker.js`
- validacao JSON de `manifest.webmanifest`
- varredura de referencias locais do HTML com query string
- varredura dos assets em `CORE_ASSETS` com query string
- validacao funcional do motor com deteccao de termos do texto

Resultado esperado:

- `decolonial-engine.js` carrega 131 entradas;
- Observador Decolonial aparece como toggle no Vocabulário Decolonizador;
- quando ligado, analisa o manuscrito ativo sem textarea duplicado;
- HTML aponta scripts e estilos para `?v=20260429-10`;
- service worker atualizado para `vereda-offline-v42`.

## Complemento: Tipos de documento no Arquivo

Rodada adicional apos criar a base leve de caderno de projeto:

- `node --check app.js`
- `node --check archive-engine.js`
- `node --check service-worker.js`
- validacao JSON de `manifest.webmanifest`
- varredura de referencias locais do HTML com query string
- varredura dos assets em `CORE_ASSETS` com query string
- validacao funcional do schema de documento com os sete tipos aceitos

Resultado esperado:

- modal "Nova nota" mostra seletor de tipo;
- Arquivo filtra por manuscrito, pesquisa, personagem, cena, mundo, cronologia e glossário;
- notas de projeto nao acionam aderencia do `precision-engine.js`;
- Espelho de Voz nao usa nota de projeto como manuscrito ativo;
- HTML aponta scripts e estilos para `?v=20260429-11`;
- service worker atualizado para `vereda-offline-v43`.

## Complemento: Checklists persistentes por guia

Rodada adicional apos transformar critérios do Guia de Escrita em checklist:

- `node --check app.js`
- `node --check service-worker.js`
- validacao JSON de `manifest.webmanifest`
- varredura de referencias locais do HTML com query string
- varredura dos assets em `CORE_ASSETS` com query string
- validacao estatica do fluxo de checklist e da chave `vereda.checklists.v1`

Resultado esperado:

- cada criterio do Guia de Escrita funciona como checkbox;
- checklist persiste em `vereda.checklists.v1[manuscriptId][templateId]`;
- Arquivo mostra progresso de manuscritos com guia como `x/y critérios`;
- HTML aponta scripts e estilos para `?v=20260429-12`;
- service worker atualizado para `vereda-offline-v44`.

## Complemento: Busca global no acervo

Rodada adicional apos adicionar busca na topbar:

- `node --check app.js`
- `node --check service-worker.js`
- validacao JSON de `manifest.webmanifest`
- varredura de referencias locais do HTML com query string
- varredura dos assets em `CORE_ASSETS` com query string
- validacao estatica de `searchAll(query)` percorrendo somente `state.manuscripts`

Resultado esperado:

- lupa da topbar expande campo de busca;
- dropdown mostra ate 8 resultados do acervo carregado;
- cada resultado retorna `title`, `content`, `type` e `updatedAt`;
- clique abre o manuscrito no editor;
- HTML aponta scripts e estilos para `?v=20260429-13`;
- service worker atualizado para `vereda-offline-v45`.

## Complemento: Guardas de autoria para notas de suporte

Rodada adicional apos separar manuscritos de notas de projeto na Autoria:

- `node --check app.js`
- `node --check version-engine.js`
- `node --check proof-engine.js`
- `node --check service-worker.js`
- validacao JSON de `manifest.webmanifest`
- varredura de referencias locais do HTML com query string
- varredura dos assets em `CORE_ASSETS` com query string
- validacao funcional de que nota de suporte nao cria prova nem versao

Resultado esperado:

- `proof-engine.js` permanece sem alteracao;
- `version-engine.js` preserva `type` no snapshot e no restore;
- prova de autoria e versoes rodam apenas para `type === "manuscrito"`;
- Autoria mostra mensagem neutra para pesquisa/personagem/cena/mundo/cronologia/glossario;
- HTML aponta scripts e estilos para `?v=20260429-14`;
- service worker atualizado para `vereda-offline-v46`.

## Complemento: Favicon da aba em PNG

Rodada adicional apos trocar o icone da aba para PNG:

- regeneracao de `tab-favicon-16x16.png`, `tab-favicon-32x32.png`, `tab-favicon-48x48.png`, `tab-favicon-180x180.png` e `tab-favicon.ico`;
- `file favicon_io/tab-favicon-* favicon_io/tab-favicon.ico`;
- `identify -format` para conferir dimensoes e transparencia dos PNGs;
- validacao JSON de `manifest.webmanifest`;
- varredura de referencias locais do HTML com query string;
- varredura dos assets em `CORE_ASSETS` com query string;
- `node --check service-worker.js`;
- `git diff --check`.

Resultado esperado:

- o `<head>` nao oferece mais SVG como favicon da aba;
- o navegador passa a preferir PNG para 48, 32 e 16 px;
- `tab-favicon.ico` e mantido como fallback;
- HTML aponta assets para `?v=20260429-15`;
- service worker atualizado para `vereda-offline-v47`.

## Complemento: Favicon redondo sem fallback ICO

Rodada adicional apos remover o fallback `.ico` da aba:

- regeneracao direta de `tab-favicon-16x16.png`, `tab-favicon-32x32.png`, `tab-favicon-48x48.png` e `tab-favicon-180x180.png` a partir de `android-chrome-512x512.png`;
- confirmacao de que o `<head>` nao referencia `.ico` nem SVG como favicon;
- `identify -format` para conferir dimensoes, transparencia e area visivel dos PNGs;
- validacao JSON de `manifest.webmanifest`;
- varredura de referencias locais do HTML com query string;
- varredura dos assets em `CORE_ASSETS` com query string;
- `node --check service-worker.js`;
- `git diff --check`.

Resultado esperado:

- a aba usa apenas PNG transparente;
- o logo preserva o desenho redondo do PNG-base;
- HTML aponta assets para `?v=20260429-16`;
- service worker atualizado para `vereda-offline-v48`;
- pendencias de produto ficam registradas no roadmap, sem implementacao nesta rodada.

## Complemento: Aviso de backup local

Rodada adicional apos adicionar o aviso de risco por cache/localStorage:

- `node --check app.js`
- `node --check service-worker.js`
- validacao JSON de `manifest.webmanifest`
- varredura de referencias locais do HTML com query string
- varredura dos assets em `CORE_ASSETS` com query string
- verificacao textual de `vereda.backup-meta.v1`, `data-backup-warning` e `renderBackupWarning`
- `git diff --check`

Resultado esperado:

- Arquivo mostra aviso quando nao ha backup registrado;
- exportar/importar `.vrda` atualiza `vereda.backup-meta.v1`;
- aviso volta quando o ultimo backup tem 7 dias ou mais;
- HTML aponta assets para `?v=20260429-17`;
- service worker atualizado para `vereda-offline-v49`.

## Complemento: Favicon redondo a partir do pacote novo

Rodada adicional apos importar os favicons novos:

- leitura de `/home/rafamass/Downloads/0bf8739534198ded2abeac46e5bfc43e.ico`;
- `identify -format` nos PNGs originais e nos PNGs finais;
- remocao do canto branco opaco com `magick -alpha set -fuzz 2% -transparent white`;
- regeneracao de `tab-favicon.ico`;
- validacao JSON de `manifest.webmanifest`;
- varredura de referencias locais do HTML com query string;
- varredura dos assets em `CORE_ASSETS` com query string;
- `node --check service-worker.js`;
- `git diff --check`.

Resultado esperado:

- canto dos PNGs finais fica transparente;
- aba usa logo redondo do pacote novo;
- HTML aponta assets para `?v=20260429-18`;
- service worker atualizado para `vereda-offline-v50`.

## Complemento: Atalhos PWA com rotas distintas

Rodada adicional apos separar destinos de Manuscrito e Academia no PWA:

- `node --check app.js`
- `node --check service-worker.js`
- validacao JSON de `manifest.webmanifest`
- verificacao textual de `start_url`, atalho `./#editor` e atalho `./#academia`
- varredura de referencias locais do HTML com query string
- varredura dos assets em `CORE_ASSETS` com query string
- `git diff --check`

Resultado esperado:

- abrir `#academia` ativa a aba Academia;
- abrir sem hash ou com hash desconhecido cai em `#editor`;
- clicar nas abas atualiza o hash;
- Manifesto aponta Manuscrito e Academia para URLs distintas;
- HTML aponta assets para `?v=20260429-19`;
- service worker atualizado para `vereda-offline-v51`.

## Complemento: Meta literário por tipo de documento

Rodada adicional apos dissolver `vereda_meta_templates.html` no projeto:

- `node --check archive-engine.js`
- `node --check app.js`
- `node --check service-worker.js`
- teste VM de `VeredaArchive.defaultMeta`, `normalizeManuscript` e `createManuscript`
- teste de migração para documento antigo sem `meta`
- validacao JSON de `manifest.webmanifest`
- varredura de referencias locais do HTML com query string
- varredura dos assets em `CORE_ASSETS` com query string
- `git diff --check`

Resultado esperado:

- documentos antigos ganham `meta` seguro ao carregar;
- documentos novos nascem com `meta` conforme o tipo;
- duplicatas preservam `type` e `meta`;
- busca do acervo considera valores de `meta`;
- HTML aponta assets para `?v=20260429-20`;
- service worker atualizado para `vereda-offline-v52`.

## Complemento: Mapa final de tipos literarios

Rodada adicional apos dissolver `vereda_types_final.html` no schema ativo:

- `node --check archive-engine.js`
- `node --check app.js`
- `node --check service-worker.js`
- teste VM de todos os `DOCUMENT_TYPES` ativos com `defaultMeta` e `createManuscript`
- teste de migracao de `mundo.meta.lugar` para `mundo.meta.nome`
- teste de preservacao de chaves antigas/desconhecidas em `meta`
- validacao JSON de `manifest.webmanifest`
- varredura de referencias locais do HTML com query string
- varredura dos assets em `CORE_ASSETS` com query string
- `git diff --check`

Resultado esperado:

- `livro` e `direitos` ficam fora dos tipos ativos;
- novos tipos criam `meta` vazio conforme schema;
- `pesquisa` absorve campos bibliograficos;
- `mundo` passa a representar sistema amplo;
- HTML aponta assets para `?v=20260430-01`;
- service worker atualizado para `vereda-offline-v53`.

## Complemento: Resumos e familias do Arquivo

Rodada adicional apos dissolver `archive-engine-meta.js` no motor atual:

- `node --check archive-engine.js`
- `node --check app.js`
- `node --check service-worker.js`
- teste VM de `docSummary`, `createDocument`, `TYPE_FAMILY`, `ARCHIVE_ORDER` e alias `fonte-jorn`
- validacao JSON de `manifest.webmanifest`
- varredura de referencias locais do HTML com query string
- varredura dos assets em `CORE_ASSETS` com query string
- `git diff --check`

Resultado esperado:

- `ato` e `personagem-roteiro` ficam ativos;
- `fonte-jorn` migra para `fonte`;
- cards do Arquivo usam resumo especifico quando `meta` tem dados;
- HTML aponta assets para `?v=20260430-02`;
- service worker atualizado para `vereda-offline-v54`.

## Complemento: Autosave externo do acervo

Rodada adicional apos dissolver `autosave_filesystem_demo.html` no fluxo real:

- `node --check filesystem-backup-engine.js`
- `node --check app.js`
- `node --check service-worker.js`
- verificacao textual dos hooks `choose-filesystem-backup`, `save-filesystem-backup` e `stop-filesystem-backup`
- validacao JSON de `manifest.webmanifest`
- varredura de referencias locais do HTML com query string
- varredura dos assets em `CORE_ASSETS` com query string
- `git diff --check`

Resultado esperado:

- File System Access API fica opcional e detectada por navegador;
- autosave externo salva backup `.vrda` completo, nao apenas texto;
- `localStorage` segue como salvamento imediato;
- Firefox/Safari recebem aviso de indisponibilidade;
- HTML aponta assets para `?v=20260430-03`;
- service worker atualizado para `vereda-offline-v55`.
